name: intern2ndtask
description: "A new Flutter project."
publish_to: 'none' # This line prevents the package from being published.

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Ensure this path is correct and the folder exists in your project directory.
  assets:
    - assets/images/
